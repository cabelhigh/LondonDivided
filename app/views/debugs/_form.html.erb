<%= form_for(debug) do |f| %>
  <% if debug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(debug.errors.count, "error") %> prohibited this debug from being saved:</h2>

      <ul>
      <% debug.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :log %>
    <%= f.text_area :log %>
  </div>

  <div class="field">
    <%= f.label :user %>
    <%= f.text_field :user %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
